<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : 【状態】<%= task.status %> 【タスク】<%= task.content %></li>
    <% end %>
</ul>

<%= link_to "新規タスクの投稿",new_task_path %>
