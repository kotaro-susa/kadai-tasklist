<h1>id: <%=@tasklist2.id %>のメッセージ編集ページ</h1>

<%= form_with(model: @tasklist2, local: true) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tasklist2s_path %>