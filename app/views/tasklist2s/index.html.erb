<h1>タスク一覧</h1>

<ul>
  <% @tasklist2s.each do |tasklist| %>
    <li><%= link_to tasklist.id,tasklist %> : 【状態】<%= tasklist.status %> 【タスク】<%= tasklist.content %></li>
    <% end %>
</ul>

<%= link_to "新規タスクの投稿",new_tasklist2_path %>
